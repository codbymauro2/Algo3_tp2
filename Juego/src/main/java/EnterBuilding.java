import MainObjects.City;

public class EnterBuilding implements Action {

    @Override
    public void perform(Timer timer) {

    }
    /*private City nextCity;
    private Timer timer;

    public EnterBuilding(City nextCity, Timer timer) {
        this.nextCity = nextCity;
        this.timer = timer;
    }

    @Override
    public void perform() {
        nextCity.selectBuilding(this.timer);
    }

     */
}

